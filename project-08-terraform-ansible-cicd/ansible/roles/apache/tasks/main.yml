---
# Install and configure Apache HTTP Server

- name: Install Apache
  apt:
    name: apache2
    state: present
    update_cache: yes

- name: Ensure Apache is enabled and running
  service:
    name: apache2
    state: started
    enabled: yes
